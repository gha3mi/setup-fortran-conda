{
  "name": "setup-fortran-conda",
  "description": "GitHub Action to install Fortran compilers with Conda or vendor packages",
  "author": "Seyed Ali Ghasemi",
  "keywords": [],
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "clean:dist": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "npm run clean:dist && ncc build index.js -o dist --license licenses.txt",
    "check-dist": "npm run build && node scripts/check-dist.js"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/exec": "^3.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "0.44.1"
  }
}
