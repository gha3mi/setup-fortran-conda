name: 'Setup Fortran Conda'
description: 'Installs and configures Fortran compilers and dependencies'
inputs:
  compiler:
    description: 'Fortran compiler to install'
    required: true
  platform:
    description: 'Operating system platform'
    required: true
  packages:
    description: 'Extra conda packages to install'
    required: false
    default: ''
runs:
  using: 'node20'
  main: 'index.js'